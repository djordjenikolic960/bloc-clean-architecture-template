import '../../../../shared/constant.dart';

enum EndpointPath {
  // TODO here add endpoint that will be used in application
  exampleEndpoint(Constant.emptyString);

  const EndpointPath(this.value);

  final String value;
}
